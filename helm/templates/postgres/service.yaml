apiVersion: v1
kind: Service

metadata:
  name: postgres
  namespace: {{ .Release.namespace }}

labels:
  {{- include "python-app.labels" . | nindent 4 }}

spec:
  type: ClusterIP

  selector:
    app: postgres

  ports:
    - port: 5432
      targetPort: 5432