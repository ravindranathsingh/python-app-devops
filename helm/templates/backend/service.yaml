apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: {{ .Release.namespace }}

labels:
  {{- include "python-app.labels" . | nindent 4 }}

spec:
  type: ClusterIP

  selector:
    app: backend

  ports:
    - port: 5000
      targetPort: 5000