apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: {{ .Release.Namespace }}

labels:
  {{- include "python-app.labels" . | nindent 4 }}

spec:
  type: ClusterIP

  selector:
    app: frontend

  ports:
    - name: http
      port: 80
      targetPort: 80